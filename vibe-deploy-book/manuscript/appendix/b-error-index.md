# 부록 B. 에러 상황별 "AI에게 이렇게 물어보세요" 인덱스

증상에서 출발해 해당 절과 첫 질문을 찾는 역색인입니다. 공통 원칙: **에러 원문 통째로 + 상황 설명** (3.6절 황금 템플릿).

## 로컬 개발 중 (3장)

| 증상 | 절 | 첫 질문 |
|---|---|---|
| `command not found` / 명령어 인식 안 됨 | 3.1 | "[윈도우/맥]인데 이 에러가 나: [원문]" |
| `localhost:8000`이 안 열림 | 3.2 | "서버 실행했는데 페이지가 안 열려. 터미널 출력: [원문]" |
| 버튼 눌러도 반응 없음 | 3.3 | "버튼이 반응 없고 F12 콘솔에 이렇게 떠: [원문]" |
| 수정 후 되던 게 안 됨 | 3.3 | "방금 수정 이후 [X]가 안 돼. 원인 찾아서 고쳐줘" |
| 껐다 켜니 메시지 사라짐 | 3.3/6.2 | (에러 아님 — 메모리 저장의 정상 동작. 7장에서 해결) |

## Git / GitHub (4장)

| 증상 | 절 | 첫 질문 |
|---|---|---|
| `Permission denied` / `403` | 4.5 | "GitHub push에서 이 에러. 인증 설정부터 다시 잡아줘: [원문]" |
| `repository not found` | 4.5 | "내 저장소는 [주소]인데 이 에러: [원문]" |
| push `rejected` | 4.5 | "push가 rejected. 안전하게 해결해줘. 로컬이 최신이야: [원문]" |
| 비밀정보를 push해버림 | 4.4 | "비밀번호 든 파일을 push했어. 키 교체부터 기록 삭제까지 순서대로" |
| .gitignore에 넣었는데 계속 올라감 | 4.4 | ".env가 이미 git에 추적되고 있어. 추적 중단시켜줘" |

## Vercel 배포 (5장)

| 증상 | 절 | 첫 질문 |
|---|---|---|
| Import 목록에 저장소 없음 | 5.2 | "Vercel에서 내 저장소가 안 보여. GitHub 권한 어떻게 열어?" |
| 빌드 실패 (빨간색) | 5.5 | "빌드 실패. 빌드 로그 전문: [원문]" |
| 페이지가 500 에러 | 5.5 | "배포는 성공인데 500. 런타임 로그: [원문]. 로컬에선 정상" |
| `ModuleNotFoundError` | 5.5 | "이 모듈 에러가 서버에서만 나. requirements.txt 확인해줘: [원문]" |
| push해도 배포 안 됨 | 5.4 | "push했는데 Vercel이 새 배포를 안 시작해. 뭘 확인해야 해?" |
| 반영이 됐다 안 됐다 | 5.4 | (에러 아님 — 브라우저 캐시. 시크릿 창 확인) |

## DB / Neon (7장)

| 증상 | 절 | 첫 질문 |
|---|---|---|
| 로컬 OK, 배포하면 DB 에러 | 7.3 | "Neon+Vercel 서버리스 조합. 배포에서만 DB 에러. 로그: [원문]" |
| `could not connect` / timeout | 7.3 | "DB 연결 timeout. DATABASE_URL은 환경변수로 있어. 로그: [원문]" |
| psycopg 설치 실패 | 7.3 | "빌드에서 psycopg 실패. requirements.txt: [내용] 로그: [원문]" |
| 접속 주소 유출함 | 7.2 | "DB 접속 주소가 노출됐어. Neon에서 비밀번호 재발급 절차 알려줘" |

## 도메인 / DNS (8~9장)

| 증상 | 절 | 첫 질문 |
|---|---|---|
| 도메인이 안 열림 | 9.7 | 9.7절 만능 진단 프롬프트 (캡처 2장 + nslookup) |
| 도메인 업체 광고 페이지가 뜸 | 9.7 | "옛 주차 레코드가 남은 것 같아. 현재 레코드 목록: [캡처]" |
| www만 되거나 루트만 됨 | 9.7 | "루트/www 중 하나만 열려. Vercel Domains 캡처: [첨부]" |
| 인증서 경고 / 자물쇠 없음 | 9.6 | "https 인증서 경고가 떠. Vercel Domains 상태: [캡처]" |
| DNS 입력 형식을 모르겠음 | 9.2 | "이 화면에 이 값을 어떻게 입력해? [업체 화면 + Vercel 정답지 캡처]" |

## 판단이 필요할 때 (에러는 아니지만)

| 상황 | 절 | 질문 |
|---|---|---|
| 새 기능이 서버리스에서 되는지 모름 | 6.4 | 서버리스 가드레일 프롬프트 (부록 A) |
| 새 서비스의 과금 방식이 의심됨 | 11.2 | "이 서비스 무료 플랜은 멈추는 방식이야 과금 방식이야? 카드 등록 필요해?" |
| 대화가 산으로 감 | 3.6 | 새 대화 열고 "처음부터 원인 진단해줘" + 파일 공유 |
